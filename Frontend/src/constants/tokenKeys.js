/**
 * Token storage keys for Frontend application
 * These keys are separate from admin to prevent conflicts
 */
export const TOKEN_KEYS = {
  ACCESS_TOKEN: 'userAccessToken',
  REFRESH_TOKEN: 'userRefreshToken',
  USER: 'userUser',
};

